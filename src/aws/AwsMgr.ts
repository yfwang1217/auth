class AwsMgr {
	static sendEmail(options) {
		// Placeholder for actual AWS SES email sending logic
		console.log(`Sending email to ${options.toAddrs} with subject "${options.subject}"`);
	}
}

export default AwsMgr;
